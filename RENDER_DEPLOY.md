# 🚀 Деплой на Render

Пошаговая инструкция по развертыванию Telegram-бота на Render.

## 📋 Подготовка

### 1. Получите все необходимые токены и ключи

**Telegram Token:**
- Откройте [@BotFather](https://t.me/BotFather) в Telegram
- Отправьте `/newbot` и создайте бота
- Скопируйте токен

**OpenAI API Key:**
- Зайдите на [platform.openai.com](https://platform.openai.com/)
- Создайте API ключ в разделе API Keys

**Google Calendar Credentials:**
- Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
- Включите Google Calendar API
- Создайте OAuth 2.0 Client ID (тип: Desktop app)
- Скачайте `credentials.json`
- Запустите локально `python3 get_google_token.py` для получения refresh token
- Скопируйте `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REFRESH_TOKEN`

## 🎯 Деплой на Render

### Шаг 1: Создайте репозиторий на GitHub

1. Создайте новый репозиторий на GitHub
2. Закоммитьте все файлы проекта (кроме `.env` - он в `.gitignore`)
3. Запушьте код в репозиторий

**Важно:** Убедитесь, что `.env` файл добавлен в `.gitignore`:

```bash
echo ".env" >> .gitignore
echo "credentials.json" >> .gitignore
echo "__pycache__/" >> .gitignore
echo "*.pyc" >> .gitignore
```

### Шаг 2: Создайте новый сервис на Render

1. Зайдите на [render.com](https://render.com/) и войдите
2. Нажмите **"New +"** → **"Background Worker"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий с ботом

### Шаг 3: Настройте сервис

**Основные настройки:**

- **Name:** `telegram-calendar-bot` (или любое другое имя)
- **Environment:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `python bot.py`

**Или используйте Docker (рекомендуется):**

- **Environment:** `Docker`
- Render автоматически обнаружит `Dockerfile`

### Шаг 4: Настройте переменные окружения

В разделе **"Environment"** добавьте следующие переменные:

```
TELEGRAM_TOKEN=ваш_telegram_токен
OPENAI_API_KEY=ваш_openai_ключ
GOOGLE_CLIENT_ID=ваш_client_id
GOOGLE_CLIENT_SECRET=ваш_client_secret
GOOGLE_REFRESH_TOKEN=ваш_refresh_token
TZ=Europe/Berlin
```

**Как добавить:**
1. В настройках сервиса найдите раздел **"Environment Variables"**
2. Нажмите **"Add Environment Variable"**
3. Добавьте каждую переменную по отдельности

### Шаг 5: Запустите деплой

1. Нажмите **"Create Background Worker"**
2. Render начнет сборку и деплой
3. Следите за логами в разделе **"Logs"**

### Шаг 6: Проверьте работу

1. Дождитесь завершения деплоя (статус "Live")
2. Откройте Telegram и найдите вашего бота
3. Отправьте команду `/start`
4. Попробуйте создать событие: `встретиться с Петей завтра в 19:00`

## 🔍 Мониторинг и логи

### Просмотр логов

1. В панели Render откройте ваш сервис
2. Перейдите в раздел **"Logs"**
3. Вы увидите все логи бота в реальном времени

### Проверка статуса

- **Live** - бот работает
- **Build failed** - ошибка сборки (проверьте логи)
- **Deploy failed** - ошибка запуска (проверьте переменные окружения)

## 🔧 Решение проблем

### Бот не запускается

**Проверьте:**
1. Все переменные окружения установлены правильно
2. Нет опечаток в токенах и ключах
3. Логи на наличие ошибок

**Частые ошибки:**
- `Переменная окружения TELEGRAM_TOKEN не установлена` → Добавьте переменную в настройках Render
- `ModuleNotFoundError` → Проверьте `requirements.txt` и логи сборки
- `Connection refused` → Проверьте интернет-соединение Render

### События не создаются в календаре

**Проверьте:**
1. Google Calendar API включен в Google Cloud Console
2. Refresh token не истек
3. OAuth consent screen настроен правильно
4. Логи на наличие ошибок Google API

### Бот не отвечает в Telegram

**Проверьте:**
1. Токен Telegram правильный
2. Бот запущен (статус "Live" в Render)
3. Логи на наличие ошибок подключения

## 📝 Обновление бота

После внесения изменений в код:

1. Закоммитьте изменения в GitHub
2. Render автоматически обнаружит изменения
3. Начнется автоматический деплой
4. Следите за логами

Или запустите деплой вручную:
- В панели Render нажмите **"Manual Deploy"** → **"Deploy latest commit"**

## 💰 Стоимость

Render предоставляет бесплатный план для Background Workers:
- 750 часов в месяц бесплатно
- Достаточно для работы бота 24/7

## 🔐 Безопасность

**Важно:**
- ✅ Никогда не коммитьте `.env` файл в git
- ✅ Используйте переменные окружения в Render
- ✅ Регулярно обновляйте зависимости
- ✅ Мониторьте логи на наличие подозрительной активности

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию Render: [render.com/docs](https://render.com/docs)

