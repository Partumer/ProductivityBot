# 🚀 Пошаговая инструкция по запуску

## Шаг 1: Установка Python и зависимостей

Убедитесь, что у вас установлен Python 3.11 или выше:

```bash
python3 --version
```

Установите зависимости:

```bash
pip3 install -r requirements.txt
```

Или если используете виртуальное окружение (рекомендуется):

```bash
python3 -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows

pip install -r requirements.txt
```

## Шаг 2: Получение Telegram токена

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Скопируйте полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 3: Получение OpenAI API ключа

1. Зайдите на [platform.openai.com](https://platform.openai.com/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте его (начинается с `sk-...`)

## Шаг 4: Настройка Google Calendar API

### 4.1. Создание проекта в Google Cloud

1. Зайдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект (или выберите существующий)
3. Включите **Google Calendar API**:
   - Перейдите в "APIs & Services" → "Library"
   - Найдите "Google Calendar API"
   - Нажмите "Enable"

### 4.2. Создание OAuth 2.0 credentials

1. Перейдите в "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "OAuth client ID"
3. Если впервые - настройте OAuth consent screen:
   - User Type: External
   - App name: любое (например, "Calendar Bot")
   - User support email: ваш email
   - Developer contact: ваш email
   - Нажмите "Save and Continue"
   - Scopes: добавьте `https://www.googleapis.com/auth/calendar`
   - Test users: добавьте ваш email
   - Нажмите "Save and Continue"
4. Создайте OAuth client ID:
   - Application type: **Desktop app**
   - Name: любое (например, "Calendar Bot Client")
   - Нажмите "Create"
5. **Скачайте JSON файл** - нажмите на иконку скачивания рядом с созданным клиентом
6. Сохраните файл как `credentials.json` в корне проекта

### 4.3. Получение Refresh Token

Запустите скрипт для получения refresh token:

```bash
python3 get_google_token.py
```

Откроется браузер, где нужно:
1. Войти в ваш Google аккаунт
2. Разрешить доступ к календарю
3. Скопируйте выведенные значения (CLIENT_ID, CLIENT_SECRET, REFRESH_TOKEN)

## Шаг 5: Создание .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```bash
TELEGRAM_TOKEN=ваш_telegram_токен
OPENAI_API_KEY=ваш_openai_ключ
GOOGLE_CLIENT_ID=ваш_client_id
GOOGLE_CLIENT_SECRET=ваш_client_secret
GOOGLE_REFRESH_TOKEN=ваш_refresh_token
TZ=Europe/Berlin
```

**Важно:** Не коммитьте файл `.env` в git! Он уже должен быть в `.gitignore`.

## Шаг 6: Проверка настроек

Запустите проверочный скрипт (опционально):

```bash
python3 check_config.py
```

## Шаг 7: Запуск бота

```bash
python3 bot.py
```

Вы должны увидеть:
```
INFO - Бот запущен...
```

Теперь откройте Telegram, найдите вашего бота и отправьте команду `/start` или просто напишите:
```
встретиться с Петей завтра в 19:00
```

## 🔧 Решение проблем

### Ошибка "Переменная окружения не установлена"
- Убедитесь, что файл `.env` существует и содержит все необходимые переменные
- Проверьте, что нет лишних пробелов вокруг `=`

### Ошибка при создании события в Google Calendar
- Проверьте, что refresh token не истек
- Убедитесь, что Google Calendar API включен
- Проверьте логи для деталей ошибки

### Бот не отвечает
- Проверьте, что токен Telegram правильный
- Убедитесь, что бот запущен (смотрите логи)
- Проверьте интернет-соединение

## 📝 Запуск в фоне (Linux/macOS)

Для запуска в фоне используйте `nohup`:

```bash
nohup python3 bot.py > bot.log 2>&1 &
```

Или используйте `screen` или `tmux`:

```bash
screen -S bot
python3 bot.py
# Нажмите Ctrl+A, затем D для отсоединения
```

